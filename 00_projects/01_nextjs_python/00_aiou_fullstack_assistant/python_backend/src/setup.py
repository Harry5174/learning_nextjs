from setuptools import setup, find_packages

setup(
    name="aiou",  # Replace with your desired package name
    version="0.1.0",  # Replace with your initial version
    packages=find_packages(),  # Automatically finds packages within src
    # Optional arguments: install_requires, description, author, etc.
)